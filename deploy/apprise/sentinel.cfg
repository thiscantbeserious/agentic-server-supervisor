# Apprise config for the "sentinel" key: POST /notify/sentinel delivers here.
#
# Seeded into the apprise-config volume once (see README in this directory).
# `sentinel notify --seed-config` writes the same content — an ops one-shot the
# runtime never invokes.
#
# NOTE: this file is a TEMPLATE. The real one lives in the apprise-config
# volume and carries the actual token. Do not commit a filled-in copy.
tgram://TELEGRAM_BOT_TOKEN/TELEGRAM_CHAT_ID

I am an autonomous Server Sentinel monitor. 

My current whitelisted commands are:
{{ autonomy.allowed_commands | join(', ') }}

Task: 
1. Scan the current operating system for installed read-only monitoring, diagnostic, and performance tools.
2. Filter for tools that are NOT already in my whitelist.
3. Prioritize standard tools like iostat, vmstat, netstat, lsof, sensors, or OS-specific ones.

Output:
Return ONLY a comma-separated list of new command names to add (e.g. "lsof,iostat,vmstat"). 
Do not include explanations or markdown.

I am an autonomous Server Sentinel monitor.

Current Allowed Roots:
{{ autonomy.allowed_roots | join(', ') }}

Task:
Identify standard system directories essential for server monitoring (logs, configs, service states) on this OS that are NOT already in my allowed list.

Output:
Return ONLY a comma-separated list of new allowed absolute paths.
Do not include explanations or markdown.
